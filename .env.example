# Kopiera till .env.local och fyll i dina varden
DB_HOST=mariadb
DB_PORT=3306
DB_USER=vpnuser
DB_PASSWORD=changeme
DB_NAME=vpn_logs

# Hur ofta dashboarden uppdaterar sig (millisekunder)
NEXT_PUBLIC_REFRESH_INTERVAL=30000
